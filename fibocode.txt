#include <stdio.h>
int main()
{
    int a, b,c,d,n,i

    printf("Enter the number of terms: ");
    scanf("%d", &n);

    printf("Fibonacci Series: ");

    for (i = 1; i <= n; ++i)
    {
        printf("%d, ",a);
        c = a+b;
        a=b
        b=c
    }
    return 0;
}